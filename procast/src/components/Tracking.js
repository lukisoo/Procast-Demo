import React from 'react';
import Statistics from '../images/statistics.svg';

const Tracking = (props) => {
        
    return (
        <div style={{width: "100%", paddingTop: "3%", display: "flex", justifyContent: "center"}}>
            <img src={Statistics}/>
        </div>
    );
};

export default Tracking;
